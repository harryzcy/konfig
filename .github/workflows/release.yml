name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

jobs:
  release:
    name: GitHub
    if: github.repository_owner == 'harryzcy'
    permissions:
      contents: write
    uses: harryzcy/github-actions/.github/workflows/release.yml@85bedbe7e622e280ceae018aecc4ed12955cf0d8 # main
